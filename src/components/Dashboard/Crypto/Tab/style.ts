import { StyleSheet } from 'react-native'

export const Style = StyleSheet.create({
    container: {
        width: '100%',
        height: 80,
        paddingHorizontal: 20,
        paddingVertical: 10
    },
    tab: {
        width: '100%',
        height: '100%',
        borderRadius: 10,
        padding: 5,
        flexDirection: 'row'
    }
});