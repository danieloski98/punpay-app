import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
    parent: {
        flex: 1,
        padding: 20
    },
    switchbutton: {
        height: 40,
        width: 70,
        borderRadius: 40,
        borderWidth: 1,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        marginBottom: 10
    }
});