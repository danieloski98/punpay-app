import { StyleSheet } from 'react-native'

export const Style = StyleSheet.create({
    parent: {
        width: '100%',
        flexDirection: 'row',
        justifyContent: 'center',
        padding: 5,
        borderRadius: 10,
        borderWidth: 1,
    },
});